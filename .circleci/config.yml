version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:current

    steps:
      - checkout
      - run:
        - name: "Say Hello"
        - command: "echo hello world"

workflows:
  - say-hello-workflow:
    jobs:
      - say-hello
